apiVersion: deploy.cloud.google.com/v1
kind: Target
metadata:
  name: prod
  annotations: {}
  labels: {}
description: prod
requireApproval: true  # Requires manual approval before deploying
gke:
  cluster: projects/$PROJECT_ID/locations/us-central1-c/clusters/cluster-2
